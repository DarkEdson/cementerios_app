import React from 'react';
import { Text, View } from 'react-native';

const calculaFecha = ({
    props,
}) => (
    <View>
        <Text>calculaFecha</Text>
    </View>
);

export default calculaFecha;
